name: Windows

on:
  push:
    branches: [master]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - name: Configure, Build and Test
        uses: CesarBerriot/cmake-configure-build-test-workflow@v1.0.0